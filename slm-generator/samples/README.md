# samples/

Drop the reference SLM PDF here (e.g. `MSC_Sem_I_Information_Security_and_Applications_Unit_1.pdf`)
for visual side-by-side checks against `backend/docx_builder.py`'s output.

Its content has already been transcribed into `../tests/golden_unit1.json`
(a representative subset — the real unit is 33 pages) and is what Phase 1's
tests build and check today; this folder just needs the actual file for
eyeballing layout/colour fidelity, which the tests can't verify.
